build_environment: Ubuntu 13.10

language: java

branches:
    only:
        - master

script:
    - gradle assemble

jdk:
    - oraclejdk8

notifications:
    email:
        on_success: never
        on_failure: always