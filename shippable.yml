build_environment: Ubuntu 14.04

language: java

branches:
    only:
        - master

jdk:
    - oraclejdk8

script:
    - sudo apt-get update && sudo apt-get install oracle-java8-installer
    - java -version
    - ./gradlew assemble

notifications:
    email:
        on_success: never
        on_failure: always